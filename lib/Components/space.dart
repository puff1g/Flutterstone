import 'package:flutter/material.dart';

class Spacingg extends StatefulWidget {
  Spacingg({Key key}) : super(key: key);

  @override
  _SpacinggState createState() => _SpacinggState();
}

class _SpacinggState extends State<Spacingg> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: SizedBox(
        height: 25,
      ),
    );
  }
}
